<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tambah Menu - Norin Cafe</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    .sidebar-gradient { background: linear-gradient(135deg, #1f2937 0%, #374151 50%, #4b5563 100%); }
    .menu-item { position: relative; overflow: hidden; }
    .menu-item::before { content: ''; position: absolute; top: 0; left: -100%; width: 100%; height: 100%; background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent); transition: left 0.5s; }
    .menu-item:hover::before { left: 100%; }
    .profile-glow { box-shadow: 0 0 30px rgba(249, 115, 22, 0.3); animation: pulse-glow 2s infinite; }
    @keyframes pulse-glow { 0%, 100% { box-shadow: 0 0 30px rgba(249, 115, 22, 0.3); } 50% { box-shadow: 0 0 40px rgba(249, 115, 22, 0.5); } }
    .glass-card { background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.2); }
    .fade-in { animation: fadeIn 0.8s ease-out; }
    @keyframes fadeIn { from{opacity:0;transform:translateY(30px)} to{opacity:1;transform:translateY(0)} }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-100 via-orange-50 to-red-50 text-gray-800 min-h-screen flex">

  <!-- Sidebar -->
  <aside class="w-64 sidebar-gradient text-white flex flex-col justify-between shadow-2xl relative overflow-hidden">
    <!-- Background Decoration -->
    <div class="absolute top-0 right-0 w-40 h-40 bg-orange-500/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-32 h-32 bg-red-500/10 rounded-full blur-2xl"></div>

    <div class="relative z-10 p-6">
      <!-- Brand Header -->
      <div class="text-center mb-8">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-orange-400 to-red-600 rounded-2xl mb-4 shadow-xl">
          <i class="fas fa-utensils text-white text-2xl"></i>
        </div>
        <h1 class="text-xl font-bold bg-gradient-to-r from-orange-300 to-red-300 bg-clip-text text-transparent">
          Norin Cafe
        </h1>
      </div>

      <!-- Admin Profile -->
      <div class="glass-card rounded-2xl p-4 mb-8">
        <div class="flex flex-col items-center">
          <div class="w-24 h-24 rounded-full bg-gradient-to-br from-orange-400 to-red-500 profile-glow flex items-center justify-center">
            <i class="fas fa-user-tie text-white text-2xl"></i>
          </div>
          <p class="font-semibold mt-3 text-lg">Admin</p>
          <div class="mt-1 inline-flex items-center space-x-2 text-xs">
            <div class="w-2 h-2 bg-green-400 rounded-full"></div>
            <span class="text-green-300">Online</span>
          </div>
        </div>
      </div>

      <!-- Menu -->
      <nav class="flex flex-col space-y-4">
        <a href="/dashboard" class="menu-item bg-white/10 px-4 py-3 rounded-xl flex items-center space-x-3 hover:bg-gradient-to-r hover:from-orange-500 hover:to-red-600 transition-all">
          <i class="fas fa-chart-pie text-lg"></i>
          <span>Dashboard</span>
        </a>
        <a href="/menu" class="menu-item bg-white/10 px-4 py-3 rounded-xl flex items-center space-x-3 hover:bg-gradient-to-r hover:from-orange-500 hover:to-red-600 transition-all">
          <i class="fas fa-utensils text-lg"></i>
          <span>Kelola Menu</span>
        </a>
        <a href="/menu/add" class="menu-item bg-gradient-to-r from-orange-500 to-red-600 text-white font-semibold px-4 py-3 rounded-xl shadow-lg flex items-center space-x-3">
          <i class="fas fa-plus text-lg"></i>
          <span>Tambah Menu</span>
        </a>
        <a href="/orders" class="menu-item bg-white/10 px-4 py-3 rounded-xl flex items-center space-x-3 hover:bg-gradient-to-r hover:from-orange-500 hover:to-red-600 transition-all">
          <i class="fas fa-shopping-cart text-lg"></i>
          <span>Pesanan</span>
        </a>
      </nav>
    </div>

    <!-- Logout -->
    <div class="relative z-10 p-6">
      <a href="/logout" class="block w-full text-center bg-gradient-to-r from-red-600 to-red-700 text-white font-semibold px-4 py-3 rounded-xl transition-all flex items-center justify-center space-x-3">
        <i class="fas fa-sign-out-alt"></i>
        <span>Logout</span>
      </a>
      <div class="text-center mt-4 text-xs text-gray-400">
        <p>&copy; 2024 Norin Cafe</p>
      </div>
    </div>
  </aside>

  <!-- Main Content -->
  <main class="flex-1 p-10 flex flex-col items-center">
    <!-- Card -->
    <div class="glass-effect w-full max-w-lg rounded-2xl shadow-2xl p-8 fade-in bg-white">
      <div class="flex items-center justify-between mb-6">
        <h2 class="text-2xl font-bold text-gray-800 flex items-center">
          <i class="fas fa-plus text-orange-500 mr-3"></i> Tambah Menu
        </h2>
        <a href="/menu" class="text-sm text-orange-600 hover:underline"><i class="fas fa-arrow-left mr-1"></i>Kembali</a>
      </div>

      <form action="/menu/add" method="POST" class="space-y-6">
        <!-- Nama Menu -->
        <div>
          <label for="name" class="block text-sm font-semibold text-gray-700">
            <i class="fas fa-utensils mr-2 text-orange-500"></i>Nama Menu
          </label>
          <input type="text" id="name" name="name" required
                 class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:border-orange-500 transition-all bg-white/90"
                 placeholder="Contoh: Nasi Goreng Spesial">
        </div>

        <!-- Harga -->
        <div>
          <label for="price" class="block text-sm font-semibold text-gray-700">
            <i class="fas fa-tags mr-2 text-orange-500"></i>Harga
          </label>
          <input type="number" id="price" name="price" required min="0"
                 class="w-full px-4 py-3 border-2 border-gray-200 rounded-xl focus:outline-none focus:border-orange-500 transition-all bg-white/90"
                 placeholder="Contoh: 20000">
        </div>

        <!-- Tombol -->
        <button type="submit"
                class="w-full bg-gradient-to-r from-orange-500 to-red-600 text-white py-3 rounded-xl font-semibold transition-all shadow-lg hover:shadow-xl">
          <i class="fas fa-save mr-2"></i> Simpan
        </button>
      </form>
    </div>
  </main>
</body>
</html>
